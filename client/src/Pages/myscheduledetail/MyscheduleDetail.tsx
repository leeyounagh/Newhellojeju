import React, { useEffect } from "react";
import { useParams } from "react-router-dom";

const MyscheduleDetail = () => {
  const { id } = useParams();

  return <div className="travelDetail1" style={{ height: "750px" }}></div>;
};

export default MyscheduleDetail;
