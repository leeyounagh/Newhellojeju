import React, { useEffect } from "react";
import { useDispatch } from "react-redux";

import { HiTrash } from "react-icons/hi";

const UserStyle = () => {
  return <div className="userstyle_body"></div>;
};

export default React.memo(UserStyle);
